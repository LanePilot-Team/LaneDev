# TDX（運輸資料流通服務）憑證範本。
#
# ⚠ 這個檔案會進版控，**只放欄位名稱、不要填值**。真正的憑證放 .env.local。
#
# 用法：複製成 .env.local 並填入你自己的值。.env.local 已被 .gitignore 擋掉，不會進版控。
#   cp .env.example .env.local
#
# 憑證來源：https://tdx.transportdata.tw/ 登入後 → 會員中心 → API 金鑰
#
# ⚠ 絕對不要加 VITE_ 前綴。Vite 會把 VITE_* 直接內嵌進前端 bundle，
#   而這個專案是靜態站台部署到 GitHub Pages，等於把金鑰公開在網路上。
#   這兩個變數只有 Node 端讀得到（scripts/tdx_client.mjs）。
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=

# Google Places UI Kit／Maps JavaScript API 瀏覽器金鑰。
# 此金鑰會出現在前端，必須在 Google Cloud 設定網站來源與 API 限制。
VITE_GOOGLE_MAPS_API_KEY=
